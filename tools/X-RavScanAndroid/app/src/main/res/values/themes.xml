<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme — actual colours come from Compose; this only kills the
         system AppCompat splash so the activity drawing isn't blocked by a
         classic ActionBar. -->
    <style name="Theme.XRavScan" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#FF050811</item>
        <item name="android:navigationBarColor">#FF050811</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
